# ORG-0000: [Pattern title]

**Status:** proposed | published | deprecated
**Category:** authentication | data | sync | ui | infra | process | other
**Reusable score:** 0–100 *(human-maintained — % of similar contexts where outcome was good)*
**Source projects:** *(count only — no names if sensitive)*
**Last validated:** YYYY-MM-DD
**Publish approver:** Architect + PO

> Copy to `ORG-xxx-{slug}.md`. Register in [pattern-index.json](pattern-index.json).
> **No PII, secrets, or customer names.**

---

## Context

Domain and constraints where this pattern applied (e.g. B2B SaaS, &lt;10k users, PDPA).

## Problem / pain pattern

What problem this addresses — generalized from P-xxx across projects.

## Decision pattern

What was typically chosen (ADR/PDR category — not project-specific IDs).

## Evidence types that supported it

| Type | Typical signal |
|------|----------------|
| interview | … |
| analytics | … |
| metrics | post-ship outcome |

## Outcome

| Metric | Typical result |
|--------|----------------|
| e.g. incident rate | low |
| e.g. time-to-ship | … |

**Outcome rating:** good | mixed | poor *(aggregate across source projects)*

## Lesson (one sentence)

## Works well when

- …

## Avoid when

- …

## Conflicts with

Other ORG-xxx IDs or known anti-patterns.

## Agent use (v5 / v6)

- **v5:** Cite as hypothesis — `confidence: low` until project `E-xxx` validates
- **v6:** May rank in recommend list when `reusable_score` ≥ `pattern-index.json` → `recommend_threshold` and context tags match

## Links

- Exported from: [learning-log.md](../../02-product/learning-log.md) *(project path at export time)*
- Example ADR category: PostgreSQL, JWT, offline-first, …
