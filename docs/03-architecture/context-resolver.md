# Moved — Context Compiler

This document was renamed. **Context resolver** (alias) is now **Context compiler** — it compiles knowledge into **action bundles**, not just context retrieval. Terminology: [design-spec.md](design-spec.md) §8.

→ **[context-compiler.md](context-compiler.md)**

Commands: `compile G-xxx` (aliases: `hydrate`, `resolve`)
