# Socratic 5-Why Architectural Dialectic: `G-MDRB-036` LQR Mathematical Rigor & DARE Optimal Control

**Document ID:** `docs/06_raw/20260912_152500_g_mdrb_036_socratic_5why.md`
**Timestamp:** `2026-09-12T15:25:00+07:00`
**Target Repository:** `/Users/batrarethsudprasert/projects/wro/pybricks-micropython`
**Target Goal:** `G-MDRB-036` (`Discrete Algebraic Riccati Equation (DARE) & Mathematically Derived Optimal LQR Tracking Controller`)
**Status:** **Certified & Linked**

---

## 1. The Socratic 5-Why Root Cause Chain

```text
Defect/Review Finding: Codex review assesses LQR control mathematics at 7.8/10 and test coverage at 7.5/10: "The implementation is labeled 'LQR,' but the control law is currently a heuristic proportional state-feedback controller... It does not calculate or solve the standard LQR problem: K = (R + B^T P B)^(-1) B^T P A."
│
├── [Why 1] Why did Codex evaluate the control law as scheduled proportional state feedback rather than LQR?
│   └── Because pbio_mdrobotbase_lqr_step() computes u_v = -k_x * e_x and u_w = -(k_y * e_y + k_theta * e_theta) with manually picked preset gains, sqrt(v/300) heuristic scaling, and a 0.35 factor, without deriving K from quadratic cost matrices Q and R.
│
├── [Why 2] Why was the controller implemented using empirical proportional feedback instead of formal LQR?
│   └── Because for differential drive unicycles, the structural form of optimal LQR feedback happens to decouple into along-track and cross-track/heading channels, so empirical gains appeared superficially identical to LQR.
│
├── [Why 3] Why is empirical proportional feedback insufficient for optimal autonomous trajectory tracking?
│   └── Because hand-tuned gains lack Pareto-optimality between tracking error convergence and actuator effort, risking motor current saturation or oscillation at un-tuned speeds.
│
├── [Why 4] Why does the controller lack discrete closed-loop eigenvalue verification?
│   └── Because previous verification only checked continuous-time Routh-Hurwitz damping ratio (zeta >= 0.05), neglecting discrete sampling effects (T_s = 5ms) where high forward speed v_r can push discrete closed-loop poles outside the unit circle (|lambda| >= 1).
│
└── [Why 5] (Root Cause) Why has the unicycle tracking controller not been grounded in a formal DARE state-space formulation?
    └── The codebase lacked an explicit discrete state-space model (A_d(v_r), B_d(v_r)), quadratic weight contracts (Q, R), and an offline or online DARE solver to compute certified optimal gains K(v_r) and verify discrete spectral radius rho(A_d - B_d K) < 1.0.
```

---

## 2. Quantitative Mathematical Bounds & Invariants

1. **Discrete State-Space Model Formulation ($T_s = 0.005\text{ s}$):**
   $$A_d(v_r) = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 1 & v_r T_s \\ 0 & 0 & 1 \end{bmatrix}, \quad B_d(v_r) = \begin{bmatrix} -T_s & 0 \\ 0 & -\frac{1}{2} v_r T_s^2 \\ 0 & -T_s \end{bmatrix}$$

2. **Discrete Algebraic Riccati Equation (DARE):**
   $$P = A_d^T P A_d - (A_d^T P B_d)(R + B_d^T P B_d)^{-1}(B_d^T P A_d) + Q$$
   $$K(v_r) = (R + B_d^T P B_d)^{-1} B_d^T P A_d$$

3. **Closed-Loop Stability Invariant:**
   $$\rho(A_d(v_r) - B_d(v_r) K(v_r)) = \max_i |\lambda_i| < 1.0, \quad \forall v_r \in [50, 800]\text{ mm/s}$$

4. **Optimal Gain Parameterization Table:**
   - Precompute optimal $K(v_r)$ across 16 velocity bins ($v_r \in [50, 800]\text{ mm/s}$) for presets `OPTIMAL_BALANCED`, `OPTIMAL_AGGRESSIVE`, `OPTIMAL_SMOOTH`.
   - Linear interpolation in $O(1)$ time with zero allocation.

---

## 3. Empirical Verification Plan (8 Codex Scenarios + Benchmark)

1. **Zero-Error Equilibrium:** $e = 0 \implies u = 0$.
2. **Sign Correctness:** Lateral error $+e_y \implies$ steering correction drives heading towards path.
3. **Bounded Control Output:** Clamping to $|v_{cmd}| \le v_{max}$ and $|\omega_{cmd}| \le \omega_{max}$.
4. **Convergence from Lateral & Heading Errors:** $y_0 = 30\text{ mm}, \theta_0 = 5^\circ \implies |y| < 1.5\text{ mm}, |\theta| < 0.5^\circ$.
5. **Oscillation-Free High-Speed Tracking:** Zero crossing rate of cross-track error $< 2$ oscillations per meter at $600\text{ mm/s}$.
6. **Gain-Schedule Continuity:** Smooth gain transitions across velocity boundaries with $C^0$ continuity.
7. **Backward Driving Invariance:** Proper sign inversion for $v_r < 0$.
8. **Wheel Saturation Safety:** Anti-windup handling when single-wheel limit $v_{wheel} = v \pm \frac{w}{2} \omega$ is reached.
9. **LQR vs. PID Benchmark:** Side-by-side simulation recording settling time, overshoot, RMS cross-track error, and total control energy $\int u^T u\, dt$.
